
const STORE='fafatraining_rebuild_v1_clean_premium';
const ASSETS=window.FAFA_ASSETS||{};
let exercises=[],programs=[],visuals={},levels={},session=[];
let state={tab:'home',program:'full_body',duration:'standard',visual:'mix',exIndex:0,setDone:0,timeLeft:0,running:false,history:[],profile:{name:'Membre 1',level:'debutant',goal:'remise_forme',place:'salle',fatigue:2,sleep:7,stress:2,injuries:[],equipment:['poids du corps','haltères','barre','banc','machine','poulie','corde','rameur','bike','extérieur','gants','sac','kettlebell','box','sled','barres','medecine ball']}};
function $(s){return document.querySelector(s)} function safe(x){return String(x??'').replace(/[<>&]/g,s=>({'<':'&lt;','>':'&gt;','&':'&amp;'}[s]))}
async function boot(){[exercises,programs,visuals,levels]=await Promise.all([fetch('data/exercises.json').then(r=>r.json()),fetch('data/programs.json').then(r=>r.json()),fetch('data/visual_map.json').then(r=>r.json()),fetch('data/level_rules.json').then(r=>r.json())]);let old=localStorage.getItem(STORE);if(old)state={...state,...JSON.parse(old)};buildSession();render()}
function save(){localStorage.setItem(STORE,JSON.stringify(state))} function currentProgram(){return programs.find(p=>p.id===state.program)||programs[0]} function level(){return levels[state.profile.level]||levels.debutant}
function readiness(){let p=state.profile,s=100-p.fatigue*12-p.stress*7+Math.max(0,p.sleep-6)*4;if(p.injuries.length)s-=8;return Math.max(30,Math.min(100,Math.round(s)))}
function sessionCount(){let c=level().counts[state.duration]||level().counts.standard;if(readiness()<60)c=Math.max(4,c-2);return c}
function isOk(e){let p=state.profile,pr=currentProgram();return pr.categories.includes(e.category)&&e.equipment.some(eq=>p.equipment.includes(eq))&&!(e.avoidIf||[]).some(i=>p.injuries.includes(i))}
function buildSession(){let pool=exercises.filter(isOk);if(pool.length<4)pool=exercises.filter(e=>currentProgram().categories.includes(e.category));let warm=pool.filter(e=>['mobility','core','cardio'].includes(e.category)).slice(0,2),main=pool.filter(e=>!['mobility'].includes(e.category)).slice(0,Math.max(2,sessionCount()-3)),fin=pool.filter(e=>['cardio','core','boxe'].includes(e.category)).slice(0,2);session=[...warm,...main,...fin].filter((v,i,a)=>a.findIndex(x=>x.key===v.key)===i).slice(0,sessionCount());state.exIndex=Math.min(state.exIndex,Math.max(0,session.length-1))}
function setTab(t){state.tab=t;if(t==='session')buildSession();save();render()}
function layout(content,side=true){return `<div class="app"><header class="top"><div class="topin"><div class="brand"><img src="${ASSETS.logo}"><div><h1>FAFATRAINING COACHING</h1><p>Force ton corps · élève ton mental</p></div></div><nav class="tabs">${['home:Accueil','session:Séance','library:Exercices','tracking:Suivi','coach:Coach'].map(v=>{let [id,l]=v.split(':');return `<button class="tab ${state.tab===id?'active':''}" onclick="setTab('${id}')">${l}</button>`}).join('')}</nav></div></header><main class="main">${side?`<div class="layout"><section>${content}</section>${sidePanel()}</div>`:content}</main><nav class="bottom">${['home:🏠:Accueil','session:▶:Séance','library:🔎:Exos','tracking:📊:Suivi','coach:👤:Coach'].map(v=>{let [id,ic,l]=v.split(':');return `<button onclick="setTab('${id}')">${ic}<small>${l}</small></button>`}).join('')}</nav></div>`}
function sidePanel(){let p=state.profile,pr=currentProgram();return `<aside class="side"><button class="btn secondary" onclick="setTab('home')">← Retour accueil</button><h2>${safe(p.name)}</h2><p class="muted">${safe(level().label)} · ${safe(goalLabel(p.goal))}</p><span class="pill p-green">${safe(p.place)}</span><span class="pill p-lime">forme ${readiness()}%</span><div class="progress"><span style="width:${readiness()}%"></span></div><hr style="border-color:var(--line)"><h3>Séance</h3><p><b>${safe(pr.name)}</b><br><span class="muted">${safe(pr.duration)}</span></p><button class="btn" onclick="setTab('session')">Lancer</button><button class="btn secondary" onclick="exportPDF()">Exporter PDF</button><button class="btn secondary" onclick="setTab('tracking')">Carré client</button></aside>`}
function goalLabel(g){return {remise_forme:'Remise en forme',perte_poids:'Perte de poids',prise_masse:'Prise de masse',performance:'Performance',bien_etre:'Bien-être'}[g]||g}
function home(){let pr=currentProgram();return layout(`<div class="hero"><div class="card heroText"><div class="tag">APP COACHING TERRAIN</div><h2>Ouvre. Lance. Coache.</h2><p>Application premium pour générer une séance adaptée au niveau, au matériel, à la fatigue et aux blessures.</p><div class="actions"><button class="btn" onclick="setTab('session')">▶ Lancer la séance</button><button class="btn secondary" onclick="setTab('coach')">Modifier profil</button></div></div><div class="card logoHero"><img src="${ASSETS.logo}"></div></div><div class="today"><span class="tag">SÉANCE DU JOUR</span><h2>${safe(pr.name)}</h2><p>${safe(pr.description)} · ${safe(pr.duration)}</p></div><div class="stats"><div class="stat"><b>${sessionCount()}</b>Exos adaptés</div><div class="stat"><b>${readiness()}%</b>Forme</div><div class="stat"><b>${exercises.length}</b>Exercices</div><div class="stat"><b>${state.history.length}</b>Faites</div></div>`)}
function getVisual(e){let v=visuals[e.key]||{};if(state.visual==='photo')return `<img class="photo" src="${v.photo}" onerror="this.src='${v.fallback}'">`;if(state.visual==='avatar')return `<img class="avatar" src="${v.avatar}" onerror="this.src='${ASSETS.avatar}'">`;return `<img class="mix" src="${v.mix}" onerror="this.remove()"><img class="photo" src="${v.photo}" onerror="this.src='${v.fallback}'"><img class="avatar" src="${v.avatar}" onerror="this.src='${ASSETS.avatar}'">`}
function sessionView(){let e=session[state.exIndex]||session[0];if(!e)return layout('<div class="card"><h2>Aucune séance disponible</h2></div>');let l=level(),reps=['cardio','mobility','boxe'].includes(e.category)?l.time:l.reps+' répétitions';return layout(`<div class="sessionLayout"><section class="exercise"><div class="visual">${getVisual(e)}<span class="vlabel pill p-green">${state.visual==='mix'?'Photo + Avatar':state.visual}</span></div><div class="exerciseInfo"><span class="pill p-green">${state.exIndex+1}/${session.length}</span><span class="pill p-blue">${safe(l.difficulty)}</span><span class="pill p-orange">${safe(e.category)}</span><h2>${safe(e.name)}</h2><p class="muted">${safe(e.muscles)}</p><div class="numbers"><div class="num"><b>${l.sets}</b>Séries</div><div class="num"><b>${safe(reps)}</b>À faire</div><div class="num"><b>${l.rest}s</b>Repos</div><div class="num"><b>${state.setDone}/${l.sets}</b>Fait</div></div><div class="coachBox"><b>Consigne simple</b><p>${safe(e.tip)}</p><b>À éviter</b><p>${safe(e.mistake)}</p></div><div class="actions"><button class="btn" onclick="startRest(${l.rest})">⏱ Démarrer repos</button><button class="btn secondary" onclick="doneSet()">✓ Série faite</button><button class="btn secondary" onclick="nextExercise()">Suivant →</button></div></div></section><aside class="timerPanel"><h2>Chrono</h2><div class="timer" id="timer">${state.timeLeft||l.rest}s</div><p class="muted">Repos automatique, lisible pendant la séance.</p><div class="actions"><button class="btn secondary" onclick="pauseTimer()">Pause</button><button class="btn red" onclick="resetTimer()">Reset</button></div><hr style="border-color:var(--line)"><h3>Visuel</h3><div class="actions"><button class="btn secondary" onclick="state.visual='photo';save();render()">Photo</button><button class="btn" onclick="state.visual='mix';save();render()">Mix</button><button class="btn secondary" onclick="state.visual='avatar';save();render()">Avatar</button></div></aside></div>`,false)}
function startRest(sec){state.timeLeft=sec;state.running=true;save();clearInterval(window._timer);window._timer=setInterval(()=>{if(!state.running)return;state.timeLeft--;let t=$('#timer');if(t)t.textContent=state.timeLeft+'s';if(state.timeLeft<=0){state.running=false;clearInterval(window._timer);save()}},1000);render()} function pauseTimer(){state.running=false;save()} function resetTimer(){state.timeLeft=level().rest;state.running=false;save();render()} function doneSet(){state.setDone++;if(state.setDone>=level().sets)nextExercise();else{state.timeLeft=level().rest;save();render()}} function nextExercise(){state.exIndex++;state.setDone=0;state.running=false;if(state.exIndex>=session.length){state.history.push({date:new Date().toLocaleDateString('fr-FR'),program:currentProgram().name,count:session.length,form:readiness()});state.exIndex=0;alert('Séance terminée ✅')}save();render()}
function library(){return layout(`<div class="card"><h2>Bibliothèque propre</h2><p class="muted">Un exercice = une fiche. Recherche simple et filtres utiles.</p><div class="form"><label>Recherche<input id="q" oninput="filterLib()" placeholder="squat, boxe, haltères..."></label><label>Catégorie<select id="cat" onchange="filterLib()"><option value="">Toutes</option>${[...new Set(exercises.map(e=>e.category))].map(c=>`<option>${c}</option>`).join('')}</select></label><label>Matériel<select id="eq" onchange="filterLib()"><option value="">Tous</option>${[...new Set(exercises.flatMap(e=>e.equipment))].sort().map(c=>`<option>${c}</option>`).join('')}</select></label></div></div><div class="grid grid2">${exercises.map(e=>`<div class="libItem" data-t="${safe((e.name+' '+e.category+' '+e.equipment.join(' ')).toLowerCase())}"><div class="thumb"><img src="${visuals[e.key]?.photo}" onerror="this.remove()">Photo</div><div><b>${safe(e.name)}</b><p class="muted">${safe(e.muscles)}</p><span class="pill p-green">${safe(e.category)}</span></div></div>`).join('')}</div>`)}
function filterLib(){let q=($('#q')?.value||'').toLowerCase(),cat=$('#cat')?.value||'',eq=$('#eq')?.value||'';document.querySelectorAll('.libItem').forEach(it=>{let txt=it.dataset.t;it.style.display=(txt.includes(q)&&(!cat||txt.includes(cat))&&(!eq||txt.includes(eq)))?'grid':'none'})}
function tracking(){return layout(`<div class="card"><h2>Suivi utile</h2><div class="stats"><div class="stat"><b>${state.history.length}</b>Séances</div><div class="stat"><b>${readiness()}%</b>Forme</div><div class="stat"><b>${session.length}</b>Exos</div><div class="stat"><b>${safe(level().label)}</b>Niveau</div></div></div><div class="card"><h2>Historique</h2>${state.history.slice().reverse().map(h=>`<p>✅ ${h.date} · ${safe(h.program)} · ${h.count} exos · forme ${h.form}%</p>`).join('')||'<p class="muted">Aucune séance terminée.</p>'}</div><div class="card"><h2>Carré client</h2><div class="poster">${posterHTML()}</div><br><button class="btn" onclick="printPoster()">Exporter carré Insta</button></div>`)}
function posterHTML(){let blocks=[['ENTRÉE','Activation',...session.slice(0,3)],['PLAT','Bloc principal',...session.slice(3,7)],['FINISHER','Explosif',...session.slice(7,10)]];return `<h2>MENU SÉANCE</h2><p>FAFATRAINING · ${safe(currentProgram().name)}</p>${blocks.map(b=>`<div class="posterRow"><div class="posterCell"><b>${b[0]}</b><br>${b[1]}</div>${b.slice(2).map(e=>`<div class="posterCell">${e?'<b>'+safe(e.name)+'</b><br>'+safe(e.muscles):'-'}</div>`).join('')}</div>`).join('')}`} function printPoster(){let w=window.open('','_blank');w.document.write(`<html><head><link rel="stylesheet" href="style.css"><style>body{background:#050805;padding:20px}.poster{width:1080px;height:1080px}</style></head><body><div class="poster">${posterHTML()}</div><script>setTimeout(()=>print(),500)<\\/script></body></html>`);w.document.close()}
function coach(){let p=state.profile;return layout(`<div class="card"><h2>Coach</h2><p class="muted">Réglages simples. L’app adapte ensuite la séance.</p><div class="form"><label>Nom<input id="name" value="${safe(p.name)}"></label><label>Niveau<select id="lvl"><option value="debutant">Débutant</option><option value="intermediaire">Intermédiaire</option><option value="avance">Avancé</option><option value="expert">Expert</option></select></label><label>Objectif<select id="goal"><option value="remise_forme">Remise en forme</option><option value="perte_poids">Perte de poids</option><option value="prise_masse">Prise de masse</option><option value="performance">Performance</option><option value="bien_etre">Bien-être</option></select></label><label>Programme<select id="program">${programs.map(pr=>`<option value="${pr.id}">${safe(pr.name)} · ${safe(pr.duration)}</option>`).join('')}</select></label><label>Durée<select id="duration"><option value="express">Express 20 min</option><option value="standard">Standard 45 min</option><option value="long">Longue 1h+</option></select></label><label>Lieu<select id="place"><option>salle</option><option>domicile</option><option>extérieur</option></select></label><label>Fatigue 1-5<input id="fatigue" type="number" min="1" max="5" value="${p.fatigue}"></label><label>Sommeil h<input id="sleep" type="number" value="${p.sleep}"></label><label>Stress 1-5<input id="stress" type="number" min="1" max="5" value="${p.stress}"></label></div><button class="btn" onclick="saveCoach()">Enregistrer & générer séance</button></div><div class="card"><h2>Blessures / limitations</h2><div class="grid grid3">${['épaule','coude','poignet','dos','lombaires','hanche','genou','cheville','cardio','nuque'].map(i=>`<label><input class="inj" type="checkbox" value="${i}" ${p.injuries.includes(i)?'checked':''}> ${i}</label>`).join('')}</div></div><div class="card"><h2>Matériel</h2><div class="grid grid3">${['poids du corps','haltères','barre','banc','machine','poulie','corde','rameur','bike','extérieur','gants','sac','kettlebell','box','sled','medecine ball','barres'].map(i=>`<label><input class="eq" type="checkbox" value="${i}" ${p.equipment.includes(i)?'checked':''}> ${i}</label>`).join('')}</div></div>`)}
function saveCoach(){let p=state.profile;p.name=name.value;p.level=lvl.value;p.goal=goal.value;p.place=place.value;p.fatigue=+fatigue.value;p.sleep=+sleep.value;p.stress=+stress.value;p.injuries=[...document.querySelectorAll('.inj:checked')].map(x=>x.value);p.equipment=[...document.querySelectorAll('.eq:checked')].map(x=>x.value);state.program=program.value;state.duration=duration.value;buildSession();save();setTab('home')}
function exportPDF(){let l=level();let rows=session.map((e,i)=>`<tr><td>${i+1}</td><td><b>${safe(e.name)}</b><br>${safe(e.muscles)}</td><td>${l.sets}</td><td>${['cardio','mobility','boxe'].includes(e.category)?l.time:l.reps}</td><td>${l.rest}s</td><td>${safe(e.tip)}</td></tr>`).join('');let html=`<html><head><title>FAFATRAINING PDF</title><style>body{font-family:Arial;padding:28px;color:#111}.head{display:flex;gap:14px;align-items:center}.head img{width:95px;height:95px;border-radius:50%;object-fit:cover}h1{margin:0;font-size:30px}.box{border:1px solid #ddd;border-radius:14px;padding:12px;margin:14px 0}table{width:100%;border-collapse:collapse;font-size:12px}th{background:#050805;color:#62ff69;padding:8px;text-align:left}td{border-bottom:1px solid #ddd;padding:8px;vertical-align:top}.green{color:#169c2a;font-weight:900}</style></head><body><div class="head"><img src="${ASSETS.logo}"><div><div class="green">FAFATRAINING COACHING</div><h1>${safe(currentProgram().name)}</h1><p>Force ton corps · élève ton mental</p></div></div><div class="box">${safe(state.profile.name)} · ${safe(l.label)} · ${safe(goalLabel(state.profile.goal))}</div><table><tr><th>#</th><th>Exercice</th><th>Séries</th><th>Répétitions</th><th>Repos</th><th>Consigne</th></tr>${rows}</table><script>setTimeout(()=>print(),500)<\\/script></body></html>`;let w=window.open('','_blank');w.document.write(html);w.document.close()}
function render(){if(state.tab==='home')app.innerHTML=home();if(state.tab==='session')app.innerHTML=sessionView();if(state.tab==='library')app.innerHTML=library();if(state.tab==='tracking')app.innerHTML=tracking();if(state.tab==='coach')app.innerHTML=coach();setTimeout(()=>{if($('#lvl'))lvl.value=state.profile.level;if($('#goal'))goal.value=state.profile.goal;if($('#place'))place.value=state.profile.place;if($('#program'))program.value=state.program;if($('#duration'))duration.value=state.duration},0)}
document.addEventListener('DOMContentLoaded',boot);if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./service-worker.js').catch(()=>{}))}
